## 1. Service & Mapper Refactoring

- [x] 1.1 Update `catalogService.ts` to map Strapi price, specs, and key features without dummy fallback multipliers.
- [x] 1.2 Update `use-products.ts` to consume `catalogService` layer using TanStack React Query (`useQuery`).

## 2. Product Details Dynamic CTA Integration

- [x] 2.1 Update `ProductDetailSection.tsx` to consume dynamic WhatsApp number from `useSiteSettings()`.

## 3. Verification

- [x] 3.1 Verify product catalog and detail pages function with pure API data and zero layout/design changes.
