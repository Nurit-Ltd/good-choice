## 1. Component Implementation

- [x] 1.1 Implement `ServiceTopSwitcherStrip.tsx` in `src/components/features/services/` with slim horizontal micro-cards, active service highlight, auto-scroll centering, and desktop arrow controls.
- [x] 1.2 Update `ServiceDetailClient.tsx` to mount `ServiceTopSwitcherStrip` between the header and hero section.
- [x] 1.3 Update `ServiceDetailSkeleton.tsx` to include the top switcher strip skeleton placeholder.

## 2. Validation & Quality Checks

- [x] 2.1 Verify responsive touch swipe and auto-scroll on mobile, tablet, and desktop viewports.
- [x] 2.2 Verify zero layout shift, fast image rendering with `sizes`, and proper route navigation on click.
- [x] 2.3 Run type check / Next.js validation to ensure zero TypeScript errors.
